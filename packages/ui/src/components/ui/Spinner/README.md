# Spinner

## Anatomy
- Spinner.tsx — composition (UI + size config)
- types.ts — SpinnerProps, SpinnerSize
- index.ts — export

## Use Cases
- Indicate loading state in buttons, forms, or page sections
- Provide accessible feedback during async operations
- Inline or overlay loading indicators
